indices:
  mysite:
    include:
      - /article-page/**
    exclude:
      - /documents/snippets/**
    target: /query-index
    properties:
      imgPath:
        select: main > img
        value: | 
          value: attribute(el, "src")